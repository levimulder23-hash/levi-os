# Workflows (WAT layer 1)

Markdown SOPs the agent reads before doing recurring work. Each file has: Objective, Inputs, Steps, Definition of Done, On failure (append to `../alerts.md`). Scripts they call live in `../scripts/`. First workflow: `daily-brief.md` (Phase 4).
