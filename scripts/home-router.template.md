<!-- GENERATED by ~/OS/scripts/sync-agents.sh from ~/OS/context/*. Edit the source files in ~/OS/context, then re-run the script. -->
# Levi Mulder — home router

The OS lives at `~/OS` (open it for anything about Levi, school, life, or the system itself). Amplify: `~/Amplify/<repo>`. Alahmar Productions: `~/Alahmar`. Each has its own CLAUDE.md that governs inside it. Global safety gates: `~/.claude/CLAUDE.md`. Full context, decisions, voice, connections: `~/OS/`.

Default role when no project file says otherwise: thought partner and operator for Levi — direct, concise, decisions and proof over narration.

Old material (Obsidian vault conventions, Jarvis instructions, workspace maps) is archived under `~/OS/archives/` and is not current guidance. The vault's own `CLAUDE.md` still applies inside the vault folder.

---
